# REDESIGN.md — サイトアップデート方針（2026-08 起案）

現行サイトをどうアップデートするかのリサーチと方針。
実装は `DESIGN.md` の改定を先に通してから着手する（CLAUDE.md § 1）。

> **改定履歴**
> - 2026-08-08 初版（コンセプト案: RUNWAY）
> - 2026-08-08 全面改稿。本番ニュースの実態調査で「ファッションショーの会社」という前提が誤りと判明
> - 2026-08-08 § 3 改稿。実態は「なんでもやってる」と判明し、コンセプト案を A/B に整理
> - 2026-08-08 全面改稿。ヒアリングで**サイトの役割そのもの**が判明。
>   コンセプト打ち出し案（A/B）は**目的に対して過剰なため全面撤回**。§ 3 で再定義。
> - 2026-08-08 **§ 3.4 追加（現行版）**。「言葉ではなく写真で語る」が方針として確定。
>   トップページが既存の写真を 96px に潰している事実（§ 1.5）を発見し、§ 4 を再構成。

---

## § 3 が最重要

このドキュメントは 2 回、前提を間違えた。
**§ 3「サイトの役割」を読まずに § 4 の施策だけ見ると、また同じ間違いをする。**

---

## § 1 現状診断

### 1.1 トップページは「ロゴ1枚 + リスト2つ」しかない

`src/components/HomeContent.tsx` の構成:

| セクション | 中身 | 実質的な情報量 |
|---|---|---|
| ヒーロー（80vh） | ネイビー背景 + 白ロゴ 400px | **ゼロ** |
| News | テキストリスト 3件 | 低 |
| Partners | ロゴのカルーセル | 低 |

**画面の8割を占めるヒーローが、テンプレートそのまま。**
ベタ塗りグラデーションの中央にロゴを置いただけで、作り込みの痕跡がない。

### 1.2 ★ 連絡先と場所が、驚くほど見つけにくい

これがこのサイトの**最大の問題**（理由は § 3 参照）。

| 情報 | 現在の置き場所 | 問題 |
|---|---|---|
| 住所 | Footer / About ページ | **トップページに無い**。最下部までスクロールが必要 |
| アクセス（最寄駅） | Footer / About | 同上 |
| 営業時間 | About ページのみ | Footer にすら無い |
| メールアドレス | Footer の1行のみ | `mailto:` リンク。トップからは遠い |
| **電話番号** | **どこにも無い** | `company.ts` に項目自体が存在しない |
| お問い合わせフォーム | **無い** | メールアプリが開くだけ |

ナビの `ACCESS`（`Navigation.tsx:49-52`）は **Google Maps への外部リンク**で、
押すとサイトから離脱する。サイト内に地図はあるのに（`GoogleMap.tsx`）、
About ページの奥に埋まっていて、ナビからは辿り着けない。

### 1.3 ⚠️ メールアドレスがプロバイダメール（要確認）

`company.ts` のメールは `sunlady2@bp.iij4u.or.jp`（IIJ4U のプロバイダメール）。
**独自ドメイン `sunlady.tokyo` を持っているのに、そちらを使っていない。**

> 直近コミット `7353a2f` で「メールアドレスを修正」しているので、
> **この値は意図的な可能性がある**。勝手に変えず § 7 で確認する。
> ただし § 3 の目的（見せて「いいね」）に対しては、
> **独自ドメインのアドレスのほうが確実に有利**。

### 1.4 ★ 制約: 画像はニュース分のみ / 更新負荷は上げられない

ヒアリングと本番調査で確定した運用の実態:

| 項目 | 実態 |
|---|---|
| 使える画像 | **ニュース記事の画像のみ**。撮り下ろしのブランド写真は無い |
| 画像の質 | iPhone スナップ（`IMG_2816.jpeg`）、イベントフライヤー（`第6回CCB.jpg`）、<br>ChatGPT 生成画像が混在。**サイズも構図もバラバラ** |
| 更新頻度 | **10ヶ月で6件**（約2ヶ月に1件）。最新は 2026/5/3 で**3ヶ月前** |
| 運用リソース | 限られている。**継続的な素材投入を前提にした設計は破綻する** |

ここから設計原則が3つ:

1. **改修はコードで完結させる。運用を増やさない。**
2. **1枚を大きく見せない。枠を揃えて複数で見せる。**
   `aspect-video` + `object-cover` で枠を固定すれば、
   iPhone スナップとイベントフライヤーが同じグリッドに並んでも成立する。
   逆に1枚を全画面に引き伸ばすと粗が出る。
   → **§ 3.4「写真で語る」を実現する具体的な作法がこれ。**
3. **日付を主役にしない。** 3ヶ月更新が無くても古く見えない構造にする
   （現状 `HomeContent.tsx:103` は日付がタイトルの**上**にあり、放置感を最も強く出している）

> **重要**: この制約は「写真が使えない」という意味ではない。
> **写真はある**（ニュース記事に6件中5件）。
> 問題は、**今のトップページがそれを 96px のサムネイルに縮めて捨てていること**（§ 1.5）。

### 1.5 ★★ トップページが、既にある写真をわざわざ捨てている

**このドキュメント中で最も費用対効果の高い発見。**

`/news` ページ（`NewsContent.tsx:88-90`）は、既に**画像グリッド**で表示している:

```
grid gap-6 md:grid-cols-2 lg:grid-cols-3   →  NewsCard（aspect-video の大きな画像）
```

一方トップページ（`HomeContent.tsx:70-113`）は、**同じ記事を別実装で描いている**:

```
divide-y のテキストリスト  →  サムネイル w-20 h-14（96×64px の切手サイズ）
```

| | トップページ | /news ページ |
|---|---|---|
| 表示形式 | テキストリスト | **画像グリッド** |
| 画像サイズ | **96×64px** | `aspect-video` の大判 |
| 使用コンポーネント | インラインで独自実装 | `NewsCard.tsx` |

**写真も、写真を大きく見せるコンポーネントも、グリッドのレイアウトも、既に存在する。**
トップページだけがそれを使わず、独自のテキストリストを書いて写真を潰している。

大阪 POPUP の様子も、バリスタ大会のフライヤーも、書道展の告知も、
**全部 96px に縮められて、文字の横の飾りになっている。**

→ § 4.7 で解消する。**`NewsCard` に差し替えるだけ**なので、コストはほぼゼロ。

### 1.6 会社が実際にやっていること（参考情報）

本番のニュース全6件から読み取れる実態:

| 日付 | タイトル | ジャンル |
|---|---|---|
| 2026/5/3 | SunladyTokyo 初大阪 POPUP STORE 開催！ | 自社ブランド物販 |
| 2025/12/22 | Emerging 恵比寿 2026 / デザイン画コンテスト | 地域 × 若手育成 |
| 2025/11/8 | 第6回 challenge coffee barista 参加チーム募集 | コーヒー |
| 2025/11/5 | 竹内朱莉 書道展　煌々舞踊　縁 ~en~ | 書道・舞踊 |

About が筆頭に掲げる「ファッションショー」は現在ほぼ稼働していない。
ヒアリングによれば実態は**「なんでもやってる」**。

> **この事実の扱いは § 3 で変わった。**
> 当初は「なんでもやる」を看板に掲げる案を推したが、
> **サイトの役割上、そこまで打ち出す必要は無い**（§ 3.3）。
> ただし About の記述が実態とズレている事実は残るので、§ 4 の「情報の正確さ」で扱う。

### 1.7 DESIGN.md の抑制的な方向性は、**正しい**

> ⚠️ **これは前版からの訂正。**
> 前版は「DESIGN.md が遊び心を禁止しているのが問題」と書いたが、
> § 3 の目的が判明した今、**この評価は誤り**だった。

現行 DESIGN.md の禁止事項（scale 禁止 / hover scale 禁止 / y offset 6px /
シャドウ禁止 / `--radius: 2px`）は、
**「見せて『いいね』と言われる」という目的に対してはむしろ正しい。**

問題は方向性ではなく**実行**。
抑制の美学は、**引き算した後に残ったものの質が高くて初めて成立する**。
現状は「ミニマル」ではなく、ただの**未完成**。
引く前の中身も、残ったものの作り込みも無い。

**したがって DESIGN.md の改定は最小限でよい**（§ 5）。緩めるのではなく、守って作り込む。

なお既存コードに違反が1件 — `PartnerCard.tsx:59-60` の `y: 20` / `delay: index * 0.05` は
§ 6 の 6px・0.04s 制限を超えている。改定時に整合させる。

### 1.8 SEO バグ

`src/app/layout.tsx:36` の `metadataBase` と `:69` の OG `url` が
`https://sunlady.co.jp` を指しているが、**このドメインは DNS 解決しない**
（`getaddrinfo ENOTFOUND` を確認済み。本番は `https://sunlady.tokyo`）。

canonical URL と OG 画像の絶対 URL が存在しないドメインを向いている。
**サイトの URL を送って見せたときに、サムネイルが出ていない可能性が高い。**
§ 3 の「仕事2」に直撃するので、単独で先に直す。

---

## § 2 リサーチ

### 2.1 HUMAN MADE（ユーザー指定の参考サイト）

- アニメーションするアヒルがヒーローの主役。ミッションステートメントを併置
- パララックス、モジュール型セクション、日英混植

**学び**: よく出来ているが、**これはブランドを語るためのサイト**。
サンレディの役割（§ 3）とは目的が違うため、**構成は真似ない**。
参考にするのは「ヒーローに作り込みがある」という一点。

### 2.2 TOGA / MARKAWARE ★このサイトが目指すべき方向

| サイト | 傾向 |
|---|---|
| [TOGA](https://toga.jp) | エレガント・洗練 |
| [MARKAWARE](https://markaware.jp) | ミニマル・スタイリッシュ |

**余白と文字組と写真の質だけで成立させている。**
主張の強いコピーもキャラクターも使わず、それでも「ちゃんとしてる」と伝わる。
**サンレディが取るべきはこの路線**（§ 3 の目的に最も合致する）。

### 2.3 読めない、GUNZE。

- 遊び心がコンセプトそのもの。写真をほとんど使わず文字とモーションだけで成立

**学び**: 素材ゼロでも面白くできる証明として有効。
ただし**周年特設サイトの型**であり、常設のコーポレートサイトには重すぎる。
サンレディでは採らない。

### 2.4 2026年のWebデザイントレンド

- **モーションは必須スキルに昇格** — ページ遷移・ホバーが「あって当然」の水準へ
  → **ここが「いいね」の分かれ目**。現状のサイトはこの水準に達していない
- **スクロール駆動アニメーション** — CSS `animation-timeline: scroll() / view()` で JS 不要
- **AI由来の均質化への反動** → 人間味・手触りが評価される
- ミニマリズムは継続。**装飾を足すのではなく、質で差をつける**

---

## § 3 サイトの役割 ★このドキュメントの前提

### 3.1 ヒアリングで判明したこと

> 「ここでアピールしたいってよりは、必要な情報が確認できたら良いんだと思う。
> 場所とか連絡先とかが。あとはそれを見せた時にいいねって言われれば良い感じだと思う」

**このサイトは名刺代わり。** 仕事は2つしかない。

| | 仕事 | 成否の判定 |
|---|---|---|
| **仕事1** | 必要な情報が確認できる | 場所・連絡先・営業時間に**何秒で辿り着けるか** |
| **仕事2** | 見せたときに「いいね」と言われる | **最初の3秒の印象** |

### 3.2 このサイトが**やらなくていいこと**

| やらないこと | 理由 |
|---|---|
| ブランドストーリーを語る | アピールの場ではない |
| 事業内容を魅力的に売り込む | 同上 |
| コンバージョン導線の最適化 | リード獲得サイトではない |
| コンテンツを増やして SEO を狙う | 更新リソースが無い（§ 1.4） |
| 「なんでもやります」の打ち出し | **§ 3.3 で撤回** |

### 3.3 撤回する: コンセプト打ち出し案（前版の案 A / 案 B）

前版は以下を推奨していたが、**全面撤回する**。

- ~~案 A「なんでもやります。55年、そうしてきました。」~~
- ~~案 B「場をつくる」/ THE STAGE~~
- ~~ヒーローで事業名が次々入れ替わるキネティックタイポ~~

**撤回理由**: どれも**アピールの装置**であって、§ 3.1 の2つの仕事のどちらでもない。
むしろ有害になりうる:

- 名刺代わりに見せる相手（既存の取引先・知人）は、**会社が何屋かを既に知っている**
- そこに「なんでもやります」を大書すると、説明過多で**うるさい**
- 3秒の第一印象を、読ませるコピーで埋めるのは逆効果

**「いいね」は、読ませて獲得するものではなく、見た瞬間に発生する。**

### 3.4 ★ 言葉ではなく、写真で語る

ヒアリングで示された、もう一つの重要な方針:

> 「あんまり言葉で言わずに、やってることの写真とかで語れるといいんだと思う」

これは § 3.3 の撤回（コピーで打ち出さない）と**完全に一貫している**。
言葉を減らすなら、代わりに何が語るのか —— **写真**。

そして § 1.5 のとおり、**その写真は既にサイトにある**。
大阪 POPUP、バリスタ大会、書道展、デザイン画コンテスト。
**トップページがそれを 96px に縮めて捨てているだけ。**

| ❌ この方向ではない | ✅ この方向 |
|---|---|
| 「なんでもやります」と書く | **やっている現場の写真を並べる** |
| 事業内容を7項目で列挙する | 写真を見れば何をやっているか分かる |
| キャッチコピーで説得する | 説明せず、見せる |

**「ファッションもコーヒーも書道もやっている」は、
文字で書くと言い訳がましいが、写真が6枚並んでいれば一目で伝わる。**

写真の質がバラバラでも問題ない。§ 1.4 の原則2（枠を揃えて複数で見せる）を守れば、
iPhone スナップもフライヤーも同じグリッドの中で成立する。

### 3.5 「遊び心・面白さ」はどこに宿るのか

当初の要望「遊び心とか面白さがほとんどなくて」への答えは、2つ:

1. **写真**（§ 3.4）— 中身が見えれば、それだけで面白い
2. **手触り** — 主張の強さではなく、触った時の質

| ❌ この方向ではない | ✅ この方向 |
|---|---|
| 目を引くコピー | 開いた瞬間の**構図の完成度** |
| 派手なアニメーション | スクロールとホバーの**反応の気持ちよさ** |
| 情報量を増やす | 余白・行間・文字サイズの**精度** |
| キャラクター・イラスト | 細部が**丁寧に作られている**という感触 |

**引き算した結果として残ったものの質を上げる。**
DESIGN.md の抑制的な方向性（§ 1.7）は、この読みと完全に一致する。

### 3.6 「見せる」の実態を想定する

「見せた時にいいねって言われれば良い」——
**この「見せる」は、ほぼ確実にスマートフォンでの提示。**
名刺代わりにサイトを見せる場面は、対面でスマホ画面を渡すか、URL を送るか。

**したがって、モバイルでの見え方が本番。**デスクトップは副。
また URL を送る場合は OG 画像が第一印象になるので、§ 1.8 の SEO バグは致命的。

---

## § 4 施策一覧

**優先度は § 3 の「仕事1 / 仕事2」への貢献で決める。**

---

### P0 — 仕事1: 必要な情報に辿り着けるようにする

#### 4.1 ★ トップページに「情報ブロック」を置く
**インパクト: 特大 / コスト: 小 / 運用コスト: ゼロ**

現状、場所も連絡先も**Footer まで到達しないと分からない**（§ 1.2）。
これがこのサイトの最大の実用上の欠陥。

- News / Partners の下、Footer の上に **Contact & Access セクション**を新設
- 内容: 住所 / 最寄駅 / 営業時間 / 連絡先 / 地図
- **地図をここに置く**（現在 `GoogleMap.tsx` は About の奥にしかない）
- データはすべて `src/lib/company.ts` から参照（CLAUDE.md § 2）

> Footer と重複するが、**この用途では重複が正しい**。
> 「連絡先どこ？」と探させないことが仕事1。

#### 4.2 ★ ナビの `ACCESS` を外部リンクからサイト内アンカーに
**インパクト: 大 / コスト: 極小**

`Navigation.tsx:49-52` の `ACCESS` は Google Maps への外部リンクで、
**押すとサイトから離脱する**。見せている最中に離脱するのは最悪。

- リンク先を 4.1 のセクション（`/#access`）または `/about#access` に変更
- Google Maps へは、そのセクション内の「地図で見る」から飛ばす（離脱を任意にする）
- 外部リンクアイコン（`ArrowTopRightOnSquareIcon`）も併せて外す

#### 4.3 ⚠️ 電話番号の追加（要確認）
**インパクト: 大 / コスト: 極小 / 判断: クライアント**

**サイト上に電話番号が一切ない**（`company.ts` に項目自体が無い）。
名刺代わりのサイトとして、これは最も基本的な欠落。

- 掲載可なら `company.ts` に `tel` を追加し、4.1 / Footer / About に反映
- モバイルでは `tel:` リンクでタップ発信できるようにする（§ 3.6）
- **非掲載が方針なら何もしない。** § 7 で確認する

#### 4.4 ⚠️ メールアドレスの独自ドメイン化（要確認）
**インパクト: 中 / コスト: 極小（DNS 設定が必要） / 判断: クライアント**

`sunlady2@bp.iij4u.or.jp` → `info@sunlady.tokyo` 等。
§ 1.3 のとおり、直近コミットで「修正」されている値なので**勝手に変えない**。

#### 4.5 ✅ SEO バグ修正
**インパクト: 中 / コスト: 極小 / 依存: なし**

`layout.tsx:36` / `:69` の `sunlady.co.jp` → `sunlady.tokyo`（§ 1.8）。
**URL を送って見せたときの第一印象に直結する。単独で今すぐ出せる。**

#### 4.6 ⚠️ About の事業内容を実態に合わせる（要確認）
**インパクト: 中 / コスト: 小 / 判断: クライアント**

`AboutContent.tsx:64-72` の7項目が実態とズレている（§ 1.6）。
**アピールのために書き換えるのではなく、情報の正確さの問題として直す。**

- 休止中の事業を外す（問い合わせ時の齟齬を防ぐ）
- 実際にやっているのに載っていないものを足す
- **絞る必要はない。** 実態が「なんでもやってる」なら、そのまま並べてよい

---

### P1 — 仕事2: 写真に語らせる ★§ 3.4 の実装

すべて**運用コスト ゼロ**。既にある写真の見せ方を変えるだけ。

#### 4.7 ★★ トップの News をテキストリスト → 画像グリッドに
**インパクト: 特大 / コスト: 極小 / 運用コスト: ゼロ**

**このドキュメント中で最も費用対効果が高い施策。**

§ 1.5 のとおり、`NewsCard.tsx`（`aspect-video` の大判画像カード）も
グリッドのレイアウトも**既に存在する**。
トップページだけが独自のテキストリストを書いて、写真を 96px に潰している。

- `HomeContent.tsx:68-118` のインライン実装を削除し、`NewsCard` に差し替える
- レイアウトは `/news` と同じ `grid gap-6 md:grid-cols-2 lg:grid-cols-3`
- 表示件数は3件のまま（グリッド1行に収まる）

**これだけで、大阪 POPUP もバリスタ大会も書道展も大判で見えるようになる。**
言葉を1文字も足さずに「やってること」が伝わる（§ 3.4）。

> 副次効果として、トップと `/news` で**同じコンポーネントを使う**ようになり、
> 実装の二重管理も解消する。

#### 4.8 ★ 表示件数を増やして「面」で見せる
**インパクト: 大 / コスト: 極小 / 運用コスト: ゼロ**

写真で語るなら、**枚数が要る**。現在トップに出しているのは3件。

- 3件 → **6件**（グリッド2行）に増やす
- 現在の全記事数が6件なので、**手持ちの写真を全部使い切る**ことになる
- 記事が増えても最新6件で自動的に入れ替わる。運用コストはゼロ

3件だと「更新が止まったお知らせ欄」に見えるが、
6件が面で並ぶと**「活動の記録」に見える**。同じデータで印象が変わる。

#### 4.9 ⚠️ 活動写真の一括提供を打診する（要確認）
**インパクト: 特大 / コスト: 小（先方の作業次第） / 判断: クライアント**

§ 1.4 で「継続的な素材投入は無理」と結論したが、
**これは「一度きりの提供」まで否定するものではない。**

> **継続投入（毎回の運用負荷）と、一括提供（一度だけの作業）は別物。**
> 過去のイベント写真をまとめて10〜20枚もらえれば、
> 「写真で語る」サイトが**その後の運用負荷ゼロで**成立する。

- ポップアップ、バリスタ大会、書道展、コンテスト会場、事務所の様子など
- 撮り下ろしは不要。**スマホで撮った既存の写真で十分**（枠を揃えれば成立する / § 1.4）
- 実現すればヒーロー直下に「活動ギャラリー」を常設できる

**§ 7 で最優先に確認する。** これが通るかどうかで、サイトの上限が変わる。

#### 4.10 ✅ 活動写真の横スクロールストリップ（4.9 が通った場合）
**インパクト: 大 / コスト: 中 / 前提: 4.9**

ヒーロー直下に、活動写真を等速で流し続ける帯を置く。

- 4.13 のパートナーマーキーと同じ実装で作れる（Swiper 設定のみ）
- **見出しもキャプションも不要。写真だけ流す**（§ 3.4）
- 4.9 が通らない場合は、ニュース画像を流用して実装する（枚数は減る）

---

### P2 — 仕事2: 手触りを上げる

すべて**運用コスト ゼロ**。コードだけで完結する。

#### 4.11 ★ ヒーローを作り込む（構成は変えない）
**インパクト: 大 / コスト: 中 / 運用コスト: ゼロ**

**構成は今のまま（ネイビー + ロゴ）でよい。** 足すのではなく、質を上げる。

> 前版はここに「静かな1行」を足す案を書いたが、
> **§ 3.4（言葉ではなく写真で語る）を受けて、文字を足す案は取り下げる。**
> ヒーローはロゴのみで受け、**すぐ下の写真に語らせる**（4.7 / 4.10）。

現状の問題は、`HomeContent.tsx:28-54` が
「ベタ塗りグラデーション + 中央にロゴ」という**テンプレートそのまま**であること。

上げるべき質:

| 要素 | 現状 | 改善 |
|---|---|---|
| ロゴのサイズ・位置 | 中央・最大 400px | **モバイルでの見え方基準で**再設計（§ 3.6） |
| 余白 | 上下中央揃えのみ | 意図した余白比率を設計する |
| グラデーション | `from-brand-navy to-brand-navy/70` の2層 | 階調をもう一段細かく。バンディングを消す |
| 登場 | opacity 0→1 のみ | 到達点は変えず、イージングと尺を詰める |
| 高さ | `60vh / 80vh` | **下に写真が続く**と分かる高さに下げる。<br>80vh は写真への到達を遅らせるので短縮を検討 |

#### 4.12 ✅ リンクホバーに下線が引かれる動き
**インパクト: 大 / コスト: 小**

現状は `hover:bg-accent/50`（背景がうっすら変わるだけ）で反応が鈍い。
**「触った時に気持ちいい」は「いいね」の主要因**（§ 3.5）。

- 擬似要素の下線を左から右へ `transition-[width]`（`transition-all` 禁止）
- 色は `--brand-red` → 差し色が初めて実際に効く
- 対象: Footer、Navigation、テキストリンク
- `globals.css` にユーティリティを1つ定義して使い回す

#### 4.13 ✅ パートナーを「止まらないマーキー」に
**インパクト: 中 / コスト: 小**

現行 `HomeContent.tsx:148-209` は矢印付き Swiper（4秒ごとにカクッと進む）。
この**カクッと止まる動き**が、作り込みの甘さとして最も目につく。

- Swiper 設定変更のみ（`autoplay: { delay: 0 }` + `speed: 5000` + linear）
- `pauseOnMouseEnter: true` は維持
- **矢印ボタンは削除できる**（引き算）
- 3ヶ月更新が無くても、ここが動いていればサイトは死んで見えない

#### 4.14 ✅ パートナーロゴのグレースケール → カラー
**インパクト: 中 / コスト: 極小**

`PartnerCard.tsx:24` に `grayscale group-hover:grayscale-0 transition-[filter]`。
**ロゴの色がバラバラでも一覧が整う** = 全体の完成度が一段上がる。

> ニュース写真には**適用しない**。写真はカラーのまま見せる（§ 3.4）。

#### 4.15 ✅ 日付を従、タイトルを主に
**インパクト: 中 / コスト: 極小**

4.7 で `NewsCard` に差し替えると、`NewsCard.tsx:56-62` も
日付がタイトルの**上**にある構造を引き継ぐ。
2ヶ月に1件の更新頻度では、この配置が最も強く放置感を出す（§ 1.4）。

- `NewsCard.tsx` 側で順序を反転: **タイトル → 日付**（日付は最小・`muted-foreground`）
- トップと `/news` の両方に一度で効く（4.7 でコンポーネントを統一したため）
- News 見出しから「最新」の語を外す

#### 4.16 ✅ ページ遷移に View Transitions
**インパクト: 中 / コスト: 中**

ニュース一覧 → 詳細で**写真が繋がって拡大する**。
4.7 でトップが画像グリッドになるため、この効果が最も映える。
派手さゼロで体験だけ上がる。**「いいね」が出やすい典型的なポイント。**
非対応ブラウザは自動フォールバック。

---

### P3 — 余力があれば

#### 4.17 ✅ モバイル実機での総点検 ★実質 P0
**インパクト: 大 / コスト: 小**

§ 3.6 のとおり**モバイルが本番**。
実機（またはデバイスエミュレーション）で全ページを見て、
タップ領域・行間・文字サイズ・写真グリッドの収まりを詰める。

> 施策としては後ろだが、**4.11 の設計判断はこれを見てから決める**。

#### 4.18 ✅ 数字で「続いていること」を示す
`1971年設立` `55年` `第6回`（バリスタ大会の継続回数）を控えめに提示。
DESIGN.md § 3 の `tabular-nums` がここで活きる。

> 写真で語り切れているなら不要。引き算の原則に従い、最後に判断する。

---

### ❌ 却下した施策

| 施策 | 却下理由 |
|---|---|
| **コンセプト打ち出し（案 A / 案 B）** | **§ 3.3 で全面撤回。**<br>アピールの装置であり、サイトの役割に合わない |
| **ヒーローのキネティックタイポ**（事業名が入れ替わる） | 同上。3秒の印象を読ませるコピーで埋めるのは逆効果 |
| **「やってきたこと」の長い羅列セクション** | 同上。アピールになる。**§ 3.4 により写真が代わりを務める** |
| **ヒーローに「静かな1行」を足す**（前版の 4.7） | **§ 3.4 を受けて取り下げ。**<br>言葉を減らすのが方針なら、ヒーローに文字を足すのは逆行 |
| **ヒーロー背景の英字マーキー**（前版の 4.14） | 同上。文字で埋める発想。写真ストリップ（4.10）が上位互換 |
| **Works（実績）セクション新設** | 素材の**継続**投入が前提。§ 1.4 で却下。<br>ただし 4.9（一括提供）が通れば実質これが実現する |
| **ニュースのカテゴリ分け** | 運用負荷が増える（§ 1.4）。仕事1にも仕事2にも効かない |
| ニュース画像を**1枚だけ全画面**ヒーローに | フライヤー・スナップ・AI生成が混在（§ 1.4）。粗が出る。<br>**枠を揃えて複数で見せる**（4.7）なら成立する |
| 55周年特設サイト | 大規模一時制作。役割に対して過剰 |
| キャラクター/マスコット・カスタムカーソル・3D | 過剰。モバイルで無意味 |

---

## § 5 DESIGN.md に必要な改定

**§ 1.7 のとおり、DESIGN.md の方向性は正しい。改定は最小限に留める。**
前版が提案していた大幅な緩和（scale 解禁、y offset 拡大等）は**撤回する**。

| 条項 | 現行 | 改定案 | 理由 |
|---|---|---|---|
| — | — | **+ 新セクション「サイトの役割」**（§ 3 を DESIGN.md にも明記） | 将来の担当者が<br>アピール向けの設計をしないように |
| — | — | **+ 新セクション「運用制約」**（§ 1.4 を明記） | 素材前提の設計を防ぐ |
| — | — | ★ **+ 新セクション「言葉ではなく写真で語る」**（§ 3.4） | **コピーで埋める改修を将来にわたって防ぐ。**<br>今回の改定で最も重要な追加 |
| § 5 | — | **+ ニュース画像の扱い**（`aspect-video` 固定 / 1枚を全画面にしない / <br>トップと `/news` で `NewsCard` を共用） | 4.7 の前提 |
| § 6 | — | **+ 継続アニメ（マーキー等）は 20–40s / linear** | 4.13 の前提 |
| § 6 | — | **+ ページ遷移（View Transitions）の規定** | 4.16 の前提 |
| § 6 scale | 禁止 | **禁止を維持。ただし `filter`（grayscale）は許可** | 4.14 の前提。scale は品を落とす |
| § 6 stagger | 0.04s | **0.04s 維持**（`PartnerCard.tsx` の 0.05s を修正） | 既存違反の是正 |
| § 7 brand-red | CTA・強調のみ | **+ ホバー下線に拡大** | 4.12。差し色が全く使われていない |
| § 5 | — | **+ ヒーローの余白規定**（モバイル基準 / 下の写真への到達を優先） | 4.11 の前提 |

**変えないもの**（ブランドの核）:
シャドウ禁止 / `--radius: 2px` / hover scale 禁止 / y offset 6px /
ネイビー基調 / 余白の広さ / 「動きで主張しない」。

---

## § 5.5 実装ログ（2026-08-08）

STEP 0 / 1 / 2 / 4 / 5 を実装済み。**DESIGN.md は実装に先立って更新済み**（CLAUDE.md § 1）。

### 実装したもの

| # | 内容 | 主な変更 |
|---|---|---|
| 4.5 | SEO: `sunlady.co.jp`（DNS 解決せず）→ `sunlady.tokyo` | `layout.tsx` |
| — | **OG 画像を新規作成**。`/og-image.jpg` は参照だけで**実体が無かった** | `app/opengraph-image.tsx`（新規・素材不要） |
| 4.7 | トップの News をテキストリスト → 画像グリッド。`NewsCard` を共用 | `HomeContent.tsx` |
| 4.8 | トップの表示件数 3 → 6件 | `page.tsx` |
| 4.15 | 日付を従、タイトルを主に | `NewsCard.tsx` |
| 4.12 | ホバー下線（`--brand-red`）。`.link-underline` を1つ定義して共用 | `globals.css` / Nav / Footer |
| 4.13 | パートナーを等速マーキーに。矢印ボタンは削除 | `HomeContent.tsx` / `globals.css` |
| 4.14 | パートナーロゴ grayscale → ホバーでカラー | `PartnerCard.tsx` |
| 4.17 | モバイル総点検（下記） | 全体 |
| 4.11 | ヒーローの作り込み | `HomeContent.tsx` |
| 4.1 | **情報ブロック（Contact & Access）新設** | `ContactAccess.tsx`（新規） |
| 4.2 | ナビ `ACCESS` を外部リンク → `/#access` | `Navigation.tsx` |

### 実装中に見つかった既存バグ（併せて修正）

| 症状 | 原因 | 影響 |
|---|---|---|
| **モバイルで文字が全部小さい**（14px 未満が 73 箇所、最小 10.5px）<br>タップ領域も 44px 未満が 5 箇所 | `html { font-size: 14px }` がモバイルにのみ適用され、<br>rem ベースの寸法が一律 12.5% 縮んでいた | § 0.3 と真逆。**16px に統一**（DESIGN.md § 3.1） |
| **埋め込み地図が違う住所を指していた**<br>（`東京都渋谷区成正西1丁目32−11`） | 座標と住所が `GoogleMap.tsx` に直書き | CLAUDE.md § 2 違反。<br>`company.addressFull` から組み立てるよう修正 |
| **ダークモードで画像なしカードが白地に白ロゴ**になり不可視 | フォールバック背景が `bg-primary`。<br>ダークモードで白寄りに反転する | CLAUDE.md § 3 違反。`bg-brand-navy` に固定 |
| ヘッダーロゴが 32×32px と極端に小さい | SVG 実寸は 150×150 の正方形なのに<br>`width={160} height={40}` と宣言していた | 実寸に修正し 44px に |
| `tsc` がビルド後に落ちる | `news/page.tsx` の `searchParams` が<br>Next.js 15 の `Promise` 型になっていない | 型注釈を修正（コードは既に `await` 済み） |
| 地図がキーボードで開けない | `onClick` 付き `div` だった | 独立した `<a>` に変更 |
| トップページに `<h1>` が無い | — | `sr-only` で追加 |
| `PartnerCard` が DESIGN.md § 6 違反 | `y: 20` / `delay: 0.05` | 6px / 0.04s に是正 |

### 計測結果

| 項目 | 実装前 | 実装後 |
|---|---|---|
| トップのニュース画像 | 96×64px | **387×218px**（モバイル 345×194px） |
| 14px 未満のテキスト | 73 箇所 | **0 箇所** |
| 44px 未満のタップ領域 | 5 箇所 | **0 箇所** |
| 最初の写真の位置（1440×900） | フォールドの **49px 下** | フォールドの **41px 上** |
| 同（1280×800） | — | 51px 上 |
| 同（1024×768 / 1920×1080） | — | 40px 上 / 211px 上 |

### 未検証（環境制約）

プレビューペインが非表示だと CSS アニメーションのクロックが止まり、
合成イベントでは `:hover` が発火しない。以下は**実機での確認が残っている**。

- パートナーマーキーの実際の流れ方と、ホバー時の一時停止
- ホバー下線のアニメーション（`::after` の幅 0 → 100%）
- `prefers-reduced-motion` 時の完全停止

いずれも DOM・計算済みスタイル上は期待値を確認済み
（`transition-timing-function: linear` / `::after` が `width 0.2s` / `--brand-red`）。

### ローカル開発について

`.env.local` が無いためローカルはモックデータで動く。
本番の実データ構成（6件・うち5件に画像）に合わせて `mock-data.ts` を作り直したので、
グリッド2行と画像未設定時のフォールバックの両方がローカルで確認できる。

---

## § 6 ロードマップ

| フェーズ | 内容 | 状態 |
|---|---|---|
| **STEP 0** | 4.5 SEO バグ修正（+ OG 画像の新規作成） | ✅ **完了** |
| **STEP 1** | 4.7 画像グリッド化 + 4.8 6件表示 + 4.15 日付の従属化 | ✅ **完了** |
| **STEP 2** | 4.12 ホバー下線 / 4.13 マーキー / 4.14 グレースケール | ✅ **完了** |
| **STEP 3** | DESIGN.md 改定（§ 0 / § 3.1 / § 3.2 / § 5.0–5.3 / § 6.1 / § 6.2） | ✅ **完了** |
| **STEP 4** | 4.17 モバイル総点検 → 4.11 ヒーローの作り込み | ✅ **完了** |
| **STEP 5** | 仕事1: 4.1 情報ブロック / 4.2 ACCESS 導線 | ✅ **完了** |
| **STEP 6** | 4.3 電話番号 / 4.4 メール / 4.6 事業内容 | ⏸ **クライアント回答待ち**（§ 7） |
| **STEP 7** | 4.10 写真ストリップ | ⏸ **4.9 の写真提供待ち**（§ 7-1） |
| **STEP 8** | 4.16 View Transitions / 4.18 数字（必要なら） | 未着手 |

詳細は § 5.5 実装ログ。

**STEP 0 → STEP 1 → STEP 2 はクライアント確認を一切待たずに進められる。**

とくに **STEP 1 が最優先**。既存の `NewsCard` に差し替えるだけで、
**トップページに写真が並ぶ**（§ 1.5 / § 3.4）。
コストに対して見た目の変化が最も大きい。

STEP 4 / 5 は独立しているため `git worktree` で並行できる。

---

## § 7 判断が必要なこと

前版の「コンセプトをどうするか」という問いは § 3.3 で消えた。
残るのは**素材と情報の正確さに関する確認だけ**。

1. ★★ **活動写真を一括で提供できるか**（4.9）— **これが最大の分岐点。**
   継続的な運用は無理でも、**過去のイベント写真を一度だけ10〜20枚**もらえないか。
   ポップアップ、バリスタ大会、書道展、コンテスト会場、事務所の様子など。
   **撮り下ろし不要。スマホで撮った既存の写真で十分。**
   → 通れば「写真で語る」サイトが、その後の運用負荷ゼロで完成する（4.10 / STEP 7）
   → 通らなくても STEP 1 でニュース写真は表に出る。**上限が変わるだけ**
2. ★ **電話番号を載せるか**（4.3）— 現在サイト上に一切ない。
   載せるなら番号を、載せない方針なら現状維持でよい。
3. **メールアドレス**（4.4）— `sunlady2@bp.iij4u.or.jp` のままでよいか、
   `sunlady.tokyo` の独自ドメインに変えたいか。
   直近コミットで「修正」された値なので、**意図があるなら現状維持で問題ない**。
4. **お問い合わせフォームは要るか** — 現在は `mailto:` のみ。
   フォームを置くと問い合わせは増えるが、**対応の手間も増える**。
   § 1.4 の運用リソースを考えると、**置かない判断も十分あり得る**。
5. **About の事業内容**（4.6）— 7項目のうち休止しているものはあるか。
   ※ 絞る必要はない。**齟齬をなくすための確認**であって、打ち出しの話ではない。

---

## § 8 参考リンク

- [Sunlady 本番サイト](https://sunlady.tokyo/) / [ニュース一覧](https://sunlady.tokyo/news)
- [TOGA](https://toga.jp) / [MARKAWARE](https://markaware.jp) — 目指す方向
- [HUMAN MADE Inc.](https://humanmade.co.jp/) — ユーザー指定の参考サイト（構成は真似ない）
- [読めない、GUNZE。](https://gunze.co.jp/130th/) — 素材ゼロで面白くする型（今回は不採用）
- [MUUUUU.ORG ファッション業界カテゴリ](https://muuuuu.org/category/industry/fashionall)
- [Figma: Top Web Design Trends for 2026](https://www.figma.com/resource-library/web-design-trends/)
- [Envato: Web design trends for 2026](https://elements.envato.com/learn/web-design-trends)
