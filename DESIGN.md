# DESIGN.md — Sunlady Home

> このファイルがデザインの唯一の正とする。実装前に必ず参照し、ここに無いルールが必要になったら先にここを更新する。

---

## § 0 サイトの役割 ★すべての判断の前提

このサイトは**名刺代わり**であり、ブランディングサイトでもリード獲得サイトでもない。
仕事は 2 つだけ。

| | 仕事 | 判定基準 |
|---|---|---|
| 仕事1 | 必要な情報が確認できる | 場所・連絡先・営業時間に**何秒で辿り着けるか** |
| 仕事2 | 見せたときに「いいね」と言われる | **最初の3秒の印象** |

**施策はこのどちらかに効くかで判断する。どちらでもないものは実装しない。**

### やらないこと

- ブランドストーリーを語る / 事業内容を売り込む
- キャッチコピーやコンセプトの打ち出し
- コンテンツを増やして SEO を狙う（更新リソースが無い / § 0.2）

### § 0.1 言葉ではなく、写真で語る

**説明を足すのではなく、やっていることの写真を見せる。**

| ❌ | ✅ |
|---|---|
| 事業内容をコピーで説明する | やっている現場の**写真を並べる** |
| ヒーローにキャッチコピーを置く | ヒーローはロゴのみ。**すぐ下の写真に語らせる** |
| 情報量を増やす | 写真を大きく、枚数を多く |

ニュース記事の画像が、この会社の活動を写した**唯一かつ十分な素材**。
文字の横の飾りにせず、**主役として扱う**。

### § 0.2 運用制約（設計の前提）

| 項目 | 実態 |
|---|---|
| 使える画像 | ニュース記事の画像のみ。撮り下ろし写真は無い |
| 画像の質 | スマホ撮影 / イベントフライヤー / 生成画像が混在。**サイズも構図もバラバラ** |
| 更新頻度 | 約2ヶ月に1件 |

したがって:

1. **改修はコードで完結させる。運用を増やす施策は採用しない**
   （新エンドポイント追加・カテゴリ付与・継続的な素材投入を前提にした設計は禁止）
2. **1枚を大きく見せない。枠を揃えて複数で見せる**
   `aspect-video` + `object-cover` で枠を固定すれば画質のばらつきは吸収できる。
   逆に1枚を全画面に引き伸ばすと粗が出る
3. **日付を主役にしない。** 更新が数ヶ月止まっても古く見えない構造にする

### § 0.3 モバイルが本番

「見せる」場面はほぼスマートフォン。**モバイルでの見え方を基準に設計する。**
URL を送って見せる場合は OG 画像が第一印象になるため、OG の欠落は致命的に扱う。

> 詳細な調査・施策の背景は `REDESIGN.md` を参照（提案書であり、正典ではない）。

---

## § 1 Visual Theme

| 項目 | 値 |
|---|---|
| プロファイル | Corporate Landing |
| スタイル | Trust & Authority — エレガントミニマル |
| ムード | 上品・温かな人とのつながり・ファッション |
| コンテンツ言語 | 日本語中心 |
| 対象端末 | 両対応（モバイルファースト） |
| ダークモード | あり |

---

## § 2 Color Tokens

### ロゴ由来のブランドカラー

| Token | Light (HSL) | Dark (HSL) | Hex 参考 |
|---|---|---|---|
| `--primary` | `234 66% 32%` | `210 40% 96%` | #1C2788 (navy) |
| `--primary-foreground` | `0 0% 100%` | `234 66% 32%` | white / navy |
| `--brand-red` | `355 100% 45%` | `355 100% 60%` | #E60012 (accent) |

### Semantic Tokens

| Token | Light | Dark | 用途 |
|---|---|---|---|
| `--background` | `0 0% 100%` | `234 50% 8%` | ページ背景 |
| `--foreground` | `234 30% 12%` | `210 40% 96%` | 本文テキスト |
| `--card` | `0 0% 100%` | `234 40% 12%` | カード背景 |
| `--card-foreground` | `234 30% 12%` | `210 40% 96%` | カード内テキスト |
| `--muted` | `220 14% 96%` | `234 35% 16%` | 薄い背景 |
| `--muted-foreground` | `220 8% 46%` | `220 10% 60%` | 補足テキスト |
| `--accent` | `234 30% 94%` | `234 35% 20%` | ホバー背景など |
| `--border` | `220 13% 91%` | `234 30% 22%` | ボーダー |
| `--ring` | `234 66% 32%` | `210 40% 80%` | フォーカスリング |
| `--destructive` | `355 100% 45%` | `355 100% 60%` | エラー・警告 |

### 禁止事項

- hex 直書き禁止（`from-[#1a237e]` のような Tailwind arbitrary value も不可）
- ブランドカラー以外の色の新規追加は DESIGN.md 更新後のみ許可

---

## § 3 Typography

| 用途 | フォント | Weight |
|---|---|---|
| 見出し (h1–h3) | Noto Sans JP | 300–500 |
| 本文 | Noto Sans JP / Inter | 400 |
| 英字装飾 | Inter | 300 |

- 本文行間: `1.7`（現状維持）
- 禁則処理: `word-break: keep-all` 適用
- 数値列: `font-variant-numeric: tabular-nums`
- 最小フォントサイズ（全端末共通）:

| 種別 | 下限 | 例 |
|---|---|---|
| **読ませるテキスト**（本文・リンク・日付・説明文） | **14px**（`text-sm`） | ニュース日付、パートナー説明、住所 |
| **ラベル**（全大文字・`tracking-widest` の短い見出し、著作権表記） | **12px**（`text-xs`） | Footer の `COMPANY`、About の項目名 |

- `text-[11px]` のような**任意の文字サイズ指定は禁止**。上の 2 段階だけを使う

### § 3.1 root font-size は 16px 固定

`html { font-size }` を端末で変えない。**モバイルも 16px。**

> かつてモバイルだけ 14px にしていたが、rem ベースの Tailwind 寸法が
> すべて 12.5% 縮み、**最も可読性が要る画面で文字が一番小さくなる**という
> 逆転が起きていた（`text-xs` = 10.5px、`h-10` = 35px）。
> § 0.3「モバイルが本番」と真っ向から矛盾するため 16px に統一した。

これにより各ユーティリティの実寸は全端末で以下に固定される。

| クラス | 実寸 | 用途 |
|---|---|---|
| `text-xs` | 12px | **本文には使わない**（§ 5.2） |
| `text-sm` | 14px | 補足・日付・フッター本文（下限） |
| `text-base` | 16px | 本文 |

### § 3.2 タップ領域は 44px 以上

モバイルが本番（§ 0.3）なので、**押せる要素は最小 44×44px** を確保する。

- アイコンボタン: `h-11 w-11`（44px）以上。`size="icon"` の既定 `h-10`（40px）では足りない
- テキストリンク: 行の高さが足りない場合は `py-*` で当たり判定を広げる
- 例外: 視覚的に隠れているスキップリンクなど、通常操作で押さない要素

---

## § 4 Component States

すべてのインタラクティブコンポーネントに以下の状態を実装する。

| State | 実装方針 |
|---|---|
| default | token のデフォルト値 |
| hover | `hover:opacity-90` または `hover:bg-accent` |
| active / pressed | `active:scale-[0.98]` |
| focus-visible | `focus-visible:ring-2 focus-visible:ring-ring` |
| disabled | `opacity-50 cursor-not-allowed pointer-events-none` |

- transition は必ずプロパティを明示: `transition-colors duration-200` / `transition-opacity duration-200`
- `transition-all` 禁止

---

## § 4.5 Shadow & Border ルール

ファッション代理店らしいシャープな印象を保つため、シャドウは原則使用しない。

| 要素 | ルール |
|---|---|
| カード | `border` のみ。`shadow-*` 禁止 |
| ホバー | border color を `primary/30` に変化させる |
| ナビゲーション | スクロール時は `border-b` のみ（shadow 禁止） |
| モーダル / Sheet | `shadow-lg` は許容（レイヤー分離の目的） |
| `--radius` | `0.125rem`（2px）。角丸は最小限 |
| ボタン（カード内） | Button コンポーネント禁止。テキストリンク（`→` 付き）を使う |

## § 5 Spacing & Layout

- スペーシングスケール: 4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96px（Tailwind デフォルト準拠）
- コンテナ最大幅: `max-w-7xl`
- セクション縦パディング: `py-12 sm:py-24`

### § 5.0 ヒーローの高さ

| 端末 | 値 |
|---|---|
| モバイル | `h-[60vh] min-h-[400px]` |
| デスクトップ | `sm:h-[65vh] sm:max-h-[640px] sm:min-h-[480px]` |

**基準: ファーストビューに最初の写真が覗くこと。**

> かつて `sm:h-[80vh]` だったが、1440×900 で最初の写真が
> **フォールドの 49px 下**に隠れ、ネイビーとロゴしか見えない状態だった。
> 「下に続きがある」と分からないヒーローは、§ 0.1（写真で語る）を殺す。
> 背の高いディスプレイで無制限に伸びないよう `max-h` で頭も抑える。

ロゴ幅は `200 / 280 / 340px`（sm / lg）。ヒーロー高さのおおむね半分に収める。

### § 5.1 ニュース画像の扱い（§ 0.1 の実装ルール）

ニュース記事の画像は**この会社の活動を伝える主素材**なので、以下を厳守する。

| ルール | 内容 |
|---|---|
| コンポーネント | **トップページと `/news` は `NewsCard` を共用する。**<br>片方だけ別実装にしない（写真が小さくなる事故の元） |
| 画像枠 | 常に `aspect-video` + `object-cover`。画像未設定でも枠は維持 |
| サムネイル化の禁止 | **一覧で画像を 100px 未満に縮小しない。**<br>文字の横に添える小さなサムネイルとして扱わない |
| 全画面化の禁止 | 1枚を全画面ヒーローに引き伸ばさない（§ 0.2-2） |
| グリッド | `grid gap-6 md:grid-cols-2 lg:grid-cols-3` |
| トップの表示件数 | **6件**（グリッド2行）。3件だと「更新の止まったお知らせ欄」に見える |
| 画像未設定時 | ネイビー背景に白ロゴのフォールバック（CLAUDE.md § 4）。<br>背景は **`bg-brand-navy` 固定**。`bg-primary` はダークモードで白寄りに反転し、<br>白抜きロゴが見えなくなる（CLAUDE.md § 3） |
| グレースケール | **禁止**。写真はカラーのまま見せる<br>（パートナー**ロゴ**のみ grayscale 可 / § 6） |

### § 5.3 情報ブロック（Contact & Access）

**仕事1（§ 0）の中核。** 場所と連絡先に何秒で辿り着けるかがこのサイトの評価軸。

| ルール | 内容 |
|---|---|
| 配置 | **トップページに必ず置く**（Partners の下、Footer の上）。<br>Footer と内容が重複するが、**この用途では重複が正しい** |
| アンカー | `id="access"` を付ける。ナビの `ACCESS` はここに飛ばす |
| 内容 | 住所 / 最寄駅 / 営業時間 / 定休日 / 連絡先 / 地図 |
| 情報源 | **すべて `src/lib/company.ts`**（CLAUDE.md § 2）。<br>住所・座標をコンポーネントに直書きしない |
| 地図 | 埋め込み URL も `company.addressFull` から組み立てる |
| 外部リンク | Google Maps へはブロック内のリンクから。<br>**ナビから直接サイト外へ飛ばさない**（見せている最中の離脱を防ぐ） |

### § 5.2 カード内の情報順序

**タイトルを主、日付を従とする**（§ 0.2-3）。

- 順序: **タイトル → 日付**
- 日付は `text-sm` / `text-muted-foreground` で控えめに置く
- **日付に `text-xs` は使わない。** 日付は「読ませるテキスト」なので下限は 14px（§ 3）
- 見出しに「最新」など鮮度を示す語を使わない

---

## § 6 Animation

ファッション代理店らしいシャープで抑制されたアニメーション。動きで主張しない。

| 項目 | 値 |
|---|---|
| duration | 0.3s（標準）/ 0.5s（ヒーローのみ） |
| easing | `easeOut` |
| y offset | 6px のみ（20px 禁止） |
| scale | 禁止（ヒーロー含む） |
| stagger | 0.04s（0.1s 禁止） |
| hover scale | 禁止（画像の scale-105 等） |
| opacity 変化 | 主役。y はあくまで添え物 |
| `prefers-reduced-motion` | 対応必須 |
| `filter`（grayscale） | **許可**。scale の代わりにホバーの反応を作る手段として使う |

### § 6.1 継続アニメーション（マーキー等）

止まらずに流れ続ける動きは、上の duration 規定とは別カテゴリとして扱う。

| 項目 | 値 |
|---|---|
| duration | **20–40s**（1周） |
| easing | **`linear` のみ**。加減速すると「止まった」印象が出る |
| ホバー | **一時停止する**（`pauseOnMouseEnter` 等） |
| `prefers-reduced-motion` | **完全停止**。静止状態でも内容が読める配置にする |

**使ってよい対象**: パートナーロゴの帯、活動写真の帯。
**使ってはいけない対象**: 本文・見出し・ナビゲーション（可読性を損なう）。

> 意図: 更新が数ヶ月止まってもサイトが死んで見えないようにするため（§ 0.2）。
> 主張のためではなく、生存感のために使う。

### § 6.2 ホバーの下線

リンクのホバーは**背景色ではなく下線**で反応させる。
`hover:bg-accent` は反応が鈍く、「触った感じ」が出ない。

| 項目 | 値 |
|---|---|
| 実装 | 擬似要素 `::after` の幅を 0 → 100% |
| transition | **`transition-[width]`**（`transition-all` 禁止） |
| duration | 0.2s |
| 色 | **`--brand-red`**（§ 7 参照） |
| 太さ | 1px |
| `prefers-reduced-motion` | アニメーションせず即時表示 |

ユーティリティ `.link-underline` を `globals.css` に1つ定義し、全箇所で使い回す。
個別に擬似要素を書かない。

---

## § 7 Do's / Don'ts

### Do
- ロゴカラー（ネイビー・レッド）を semantic token 経由で使う
- 余白を広めに取り、上品な印象を保つ
- ヒーローのグラデーションは `from-primary to-primary/80` などトークンベースで
- アクセントカラー（`--brand-red`）は **CTA・強調ポイント・ホバーの下線**に絞る
  （面で塗らない。線と点でのみ使う）
- ホバーの反応は `filter`（grayscale → カラー）と下線で作る。scale は使わない

### Don't
- hex 直書き・arbitrary value による色指定
- `transition-all` の使用
- emoji をアイコンとして使用
- ダークモードの検証を省略する
- ブランド以外の新色を無断追加する

---

## § 8 Dark Mode

- ダークモードはクラスベース（`class` strategy）
- 背景は深いネイビー（`hsl(234 50% 8%)`）— 黒ではなくブランドと連動した色
- ロゴ: `dark:brightness-0 dark:invert` でモノクロ白抜き（現状維持）
- ヒーロー: ダーク時も視認性を保つ（グラデーション opacity 調整）

---

## § 9 Agent Prompt Guide

このプロジェクトで UI を変更するとき、AIへの指示に含めること:

```
DESIGN.md を読み、§2 のトークンのみを使って実装する。
hex 直書き・transition-all・任意の新色追加は禁止。
実装後は §4（states）と §7（Do/Don't）で self-review し、準拠確認済みとして完成を報告する。
```
